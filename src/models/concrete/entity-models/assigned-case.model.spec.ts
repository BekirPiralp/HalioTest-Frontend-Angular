import { AssignedCaseModel } from './assigned-case.model';

describe('AssignedCaseModel', () => {
  it('should create an instance', () => {
    expect(new AssignedCaseModel()).toBeTruthy();
  });
});
