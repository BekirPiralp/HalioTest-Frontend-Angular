import { DefaultFilterModel } from './default-filter-model';

describe('DefaultFilterModel', () => {
  it('should create an instance', () => {
    expect(new DefaultFilterModel()).toBeTruthy();
  });
});
