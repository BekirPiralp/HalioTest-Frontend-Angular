import { CaseStatusDescriptionFilterModel } from './case-status-description-filter-model';

describe('CaseStatusDescriptionFilterModel', () => {
  it('should create an instance', () => {
    expect(new CaseStatusDescriptionFilterModel()).toBeTruthy();
  });
});
