import { AssignedUserFilterModel } from './assigned-user-filter-model';

describe('AssignedUserFilterModel', () => {
  it('should create an instance', () => {
    expect(new AssignedUserFilterModel()).toBeTruthy();
  });
});
