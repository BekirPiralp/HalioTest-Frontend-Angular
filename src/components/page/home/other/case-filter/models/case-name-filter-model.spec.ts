import { CaseNameFilterModel } from './case-name-filter-model';

describe('CaseNameFilterModel', () => {
  it('should create an instance', () => {
    expect(new CaseNameFilterModel()).toBeTruthy();
  });
});
