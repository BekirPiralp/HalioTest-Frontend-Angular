import { OpenedUserFilterModel } from './opened-user-filter-model';

describe('OpenedUserFilterModel', () => {
  it('should create an instance', () => {
    expect(new OpenedUserFilterModel()).toBeTruthy();
  });
});
